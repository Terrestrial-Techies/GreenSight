services:
  - type: web
    name: greensight-backend 
    runtime: docker 
    plan: free 
    envVars:
      - key: PORT
        value: 5000
services:
  - type: web
    name: greensight-backend
    runtime: docker
    plan: free
    envVars:
      - key: PORT
        value: 5000
